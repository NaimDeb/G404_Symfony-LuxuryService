{% extends 'base.html.twig' %}

{% block base_url %}
	http://www.fgtechs.fr/z-pedro/wwwluxuryservices
{% endblock %}
{% block csrf_token %}
	9Tnm0YBCxgVTxoqI1HzccsU5NuE9RughZmhpzgXn
{% endblock %}
{% block favicon %}
	assets/img/ico/fav.png
{% endblock %}

{% block style %}
	<style>
		.page-title-bg {
			background-image: url('../../assets/img/bg3.jpg');
		}
		.log-section form .checkbox label {
			font-size: 16px
		}
		.log-section form .checkbox label a {
			color: #b6a575;
			text-decoration: underline;
			text-transform: inherit;
			font-size: 16px
		}
		.log-section form .links a {
			display: block;
			text-align: center;
			color: #b6a575;
			text-decoration: underline;
			text-transform: inherit;
			font-size: 16px
		}
	</style>
{% endblock %}


{% block body %}
	<!-- Page Header-->
	<section class="page-title page-title-bg fixed-bg overlay dark-5 padding-top-160 padding-bottom-80">
		<div class="container">
			<div class="row">
				<div class="col-md-12">
					<h2 class="white-text">
						Register
					</h2>
					<span class="white-text">
						Welcome on board
					</span>
					<ol class="breadcrumb">
						<li>
							<a href="#!">
								Home
							</a>
						</li>
						<li class="active">
							Register
						</li>
					</ol>
				</div>
			</div>
		</div>
	</section>

	<!-- Page Content-->
	<section class="section-padding gray-bg log-section">
		<div class="container">
			<div class="row">
				<div class="col-xs-12 col-sm-6 col-sm-offset-3 card card-panel">
					<h3 class="text-extrabold">
						Create a new account
					</h3>

					{% for flash_error in app.flashes('verify_email_error') %}
						<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
					{% endfor %}



					{{ form_start(registrationForm, {
                    'attr': {
                        'id': 'register-form',
                        'role': 'form',
                        'method': 'post',
                        'autocomplete': 'off',
                    }
                }) }}


						{{ form_errors(registrationForm.email , {'attr': {'class': 'alert alert-danger'}} ) }}
						{{ form_errors(registrationForm.plainPassword.first , {'attr': {'class': 'alert alert-danger'}} ) }}
						{{ form_errors(registrationForm.plainPassword.second , {'attr': {'class': 'alert alert-danger'}} ) }}
						{{ form_errors(registrationForm.agreeTerms , {'attr': {'class': 'alert alert-danger'} } ) }}


					<div class="input-field">
					
						{{ form_widget(registrationForm.email)}}
						{{ form_label(registrationForm.email) }}
						<span class="help-block">
							Type your email address.
						</span>
					</div>

					<div class="input-field">
                        
						{{ form_widget(registrationForm.plainPassword.first, {
                        'attr': {
                            'required': 'required',
                        }
                    }) }}
						{{ form_label(registrationForm.plainPassword.first, 'Password') }}
						<i class="fa fa-eye show-password"></i>
						<span class="help-block">
							Type your password.
						</span>
					</div>

					<div class="input-field">
                        
						{{ form_widget(registrationForm.plainPassword.second, {
                        'attr': {
                            'required': 'required',
                        }
                    }) }}
						{{ form_label(registrationForm.plainPassword.second, 'Confirm Password') }}
						<i class="fa fa-eye show-password"></i>
						<span class="help-block">
							Confirm your password.
						</span>
					</div>

					<div class="checkbox">
                        
						{{ form_widget(registrationForm.agreeTerms) }}
						{{ form_label(registrationForm.agreeTerms)}}
					</div>

					<button type="submit" class="btn btn-lg gradient secondary btn-block waves-effect waves-light mt-20 mb-20">
						Create an account
					</button>

					<div class="links">
						<a href="{{ path('app_login') }}">
							Already have an account? Click here
						</a>
					</div>

					{{ form_end(registrationForm) }}
				</div>
			</div>
		</div>
	</section>
{% endblock %}


{% block deferscripts %}
{% endblock %}
